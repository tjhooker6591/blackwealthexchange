import Link from "next/link";import { useState } from "react";
export default function My(){const [email,setEmail]=useState('');const [rows,setRows]=useState<any[]>([]);async function load(){const d=await (await fetch(`/api/support/my-tickets?email=${encodeURIComponent(email)}`)).json();setRows(d.rows||[])}
return <main className="min-h-screen bg-black text-white p-8"><div className="max-w-5xl mx-auto space-y-3"><h1 className="text-3xl font-bold text-yellow-400">My Tickets</h1><p className="text-zinc-400">Login is recommended. You can also lookup by email safely.</p><div className="flex gap-2"><input className="flex-1 p-2 bg-zinc-900 border border-zinc-700 rounded" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><button onClick={load} className="px-3 py-2 bg-yellow-500 text-black rounded">Lookup</button></div><div className="rounded border border-zinc-800 bg-zinc-950 p-4 overflow-auto"><table className="min-w-full text-sm"><thead><tr className="text-zinc-400"><th className="p-2 text-left">Ticket</th><th className="p-2 text-left">Subject</th><th className="p-2 text-left">Category</th><th className="p-2 text-left">Priority</th><th className="p-2 text-left">Status</th></tr></thead><tbody>{rows.map((r:any)=><tr key={r.ticketId} className="border-t border-zinc-800"><td className="p-2"><Link className="underline text-yellow-300" href={`/support/tickets/${encodeURIComponent(r.ticketId)}`}>{r.ticketId}</Link></td><td className="p-2">{r.subject}</td><td className="p-2">{r.category}</td><td className="p-2">{r.priority}</td><td className="p-2">{r.status}</td></tr>)}</tbody></table></div></div></main>}