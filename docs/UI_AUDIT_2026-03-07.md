# UI Audit & Enhancement Notes (2026-03-07)

## Scope audited
- Homepage search handoff (`src/pages/index.tsx`)
- Directory search UX + URL sync (`src/pages/business-directory.tsx`)
- Search API behavior and ranking (`src/pages/api/searchBusinesses.js`)
- Shared query state consistency (`src/lib/directory/queryState.js`)

## Improvements completed in this pass

### 1) Query/state consistency hardening
- Centralized homepage + directory query construction in `src/lib/directory/queryState.js`.
- Homepage now uses `buildHomepageDirectoryQuery`.
- Directory URL sync now uses `buildDirectoryUrlQuery`.
- Normalization (`scope`, `sort`) unified across pages.

### 2) End-to-end query flow check
- Added `scripts/test-directory-query-state.mjs`.
- Added npm script: `npm run check:directory-query`.
- Validates:
  - alias normalization (`org` → `organizations`, `recent` → `newest`)
  - homepage query payload shape
  - directory URL cleanup for defaults
  - filter-only URL persistence

### 3) Search ranking quality and stability
- Moved relevance/completeness ranking into Mongo aggregation pipeline.
- Ranking now happens before pagination globally (stable deep pages).
- Optional sponsor boost remains in-pipeline (`sponsoredFirst`).

### 4) Directory UI/UX improvements
- Added visible filter controls panel for sort/state/verified/sponsored.
- Added active filter chips for quick context.
- Added `Clear filters` action for one-click reset.
- Added improved toggle button accessibility labels.

## Audit findings (remaining opportunities)

### High impact next
1. **Dedicated state dropdown**
   - Replace free-text 2-letter input with a US state dropdown to eliminate invalid entries.
2. **Server-side relevance telemetry**
   - Optionally include a debug flag in non-prod to expose ranking metadata for tuning.
3. **Directory loading skeletons**
   - Replace spinner-only waits with skeleton cards for better perceived performance.

### Medium impact
4. **Keyboard affordance polish**
   - Add Enter-key submit hint and clear focus outlines for all filter chips/toggles.
5. **Saved search presets**
   - Persist filter combinations in URL shortcuts or local storage.
6. **Facet counts**
   - Show category/state counts from API for more informed filter choices.

### Tech debt / quality
7. **Consolidate API search endpoints**
   - Consider deprecating overlapping endpoints in favor of a single contract.
8. **Add integration tests (Playwright/Cypress)**
   - Cover homepage→directory handoff, filter-only search, pagination retention.
9. **Address repo-wide lint warnings**
   - Existing warnings are unrelated to this area but should be cleaned for signal clarity.

## Validation status
- `npm run check:directory-query` passed
- `npm run lint` passed with pre-existing warnings only
- `npm run build` passed
